{
  "$schema": "https://ui.shadcn.com/schema/registry.json",
  "items": [
    {
      "name": "setup",
      "type": "registry:item",
      "title": "Setup",
      "description": "Base setup for Retuned UI: the light-first amber theme (Tailwind CSS v4 tokens for colors and typography, with a built-in dark variant activated via data-theme=\"dark\") and the cn class-merging utility. Required by every component in this registry.",
      "dependencies": ["clsx", "tailwind-merge"],
      "files": [
        {
          "path": "style.css",
          "type": "registry:file",
          "target": "~/theme.css"
        },
        {
          "path": "utils/cn.ts",
          "type": "registry:file",
          "target": "~/utils/cn.ts"
        }
      ]
    }
  ]
}
