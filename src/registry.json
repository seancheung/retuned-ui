{
  "$schema": "https://ui.shadcn.com/schema/registry.json",
  "items": [
    {
      "name": "setup",
      "type": "registry:item",
      "title": "Setup",
      "description": "Base setup for Retuned UI: the dark-first amber theme (Tailwind CSS v4 tokens for colors and typography, with a built-in light variant activated via data-theme=\"light\") and the cn class-merging utility. Required by every component in this registry.",
      "dependencies": ["clsx", "tailwind-merge"],
      "files": [
        {
          "path": "style.css",
          "type": "registry:file",
          "target": "~/theme.css"
        },
        {
          "path": "utils/cn.ts",
          "type": "registry:file",
          "target": "~/utils/cn.ts"
        }
      ]
    }
  ]
}
